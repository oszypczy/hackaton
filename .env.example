# Copy to .env and fill in your own values. Never commit .env.
# GitHub PAT — read-only, scope: public_repo
# Generate: github.com → Settings → Developer settings → PAT (classic) → public_repo
GITHUB_TOKEN=ghp_your_token_here
